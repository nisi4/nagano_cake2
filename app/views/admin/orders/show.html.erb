<div class="container-fluid">
  <div class="row my-3 offset-sm-2">
    <h3 class="px-4" style="background-color:#e9ecef;">注文履歴詳細</h3>
  </div>
  <div class="row offset-1">
    <div class="col-sm-10">
       <table class="table table-borderless">
          <colgroup>
            <col width="20%">
            <col width="80%">
          </colgroup>
          <tr>
            <th>購入者</th>
            <td><%= @order.customer.full_name %></td>
          </tr>
          <tr>
            <th>注文日</th>
            <td><%= @order.created_at.strftime("%Y/%m/%d") %></td>
          </tr>
          <tr>
            <th>配送先</th>
            <td>〒<%= @order.postal_code %> <%= @order.address %><br><%= @order.name %></td>
          </tr>
          <tr>
            <th>支払方法</th>
            <td><%= @order.payment_method_i18n %></td>
          </tr>
          <tr>
            <th>注文ステータス</th>
            <td>
              <%= form_with model: @order,url: admin_order_path(@order.id),method: :patch do |f| %>
                <%= f.select :order_status,Order.order_statuses.keys.map { |k| [t("enums.order.order_status.#{k}"),k] },{},selected: @order.order_status_before_type_cast %> <%= f.submit "更新",class: "btn btn-success btn-sm text-white" %>
              <% end %>
            </td>
          </tr>
       </table>
    </div>
  </div>
  <div class="row offset-1">
    <div class="col-sm-8">
       <table class="table border-bottom">
          <colgroup>
            <col width="">
            <col width="">
            <col width="">
            <col width="">
            <col width="">
          </colgroup>
          <thead>
             <tr class="bg-light">
               <th>商品名</th>
               <th>単価（税込）</th>
               <th>数量</th>
               <th>小計</th>
               <th>製作ステータス</th>
             </tr>
          </thead>
          <tbody>
             <% @order_items.each do |order_item| %>
             <tr>
               <td><%= order_item.item.name %></td>
               <td><%= order_item.price.to_s(:delimited) %></td>
               <td><%= order_item.amount %></td>
               <td><%= order_item.subtotal.to_s(:delimited) %></td>
               <td>
                 <%= form_with model: order_item,url: admin_order_items_update_path,method: :patch do |f| %>
                   <%= f.select :product_status,OrderItem.product_statuses.keys.map { |k| [t("enums.order_item.product_status.#{k}"),k] },{},selected: order_item.product_status_before_type_cast %> <%= f.submit "更新",class: "btn btn-success btn-sm text-white" %>
                 <% end %>    
               </td>
               <% @total_price += order_item.subtotal %>
             </tr>
             <% end %>
          </tbody>
       </table> 
    </div>
    <div class="col-sm-3 mt-5 ml-5 pt-5 pl-5">
       <table class="table table-borderless">
          <colgroup>
            <col class="font-weight-bold">
            <col >
          </colgroup>
          <tr>
            <th>商品合計</th>
            <td align="right"><%= @total_price.to_s(:delimited) %>円</td>
          </tr>
          <tr>
            <th>送料</th>
            <td align="right"><%= @postage %>円</td>
          </tr>
          <tr>
            <th>請求金額合計</th>
            <td class="font-weight-bold" align="right"><%= (@total_price + @postage).to_s(:delimited) %>円</td>
          </tr>
       </table> 
    </div>
  </div>
</div>